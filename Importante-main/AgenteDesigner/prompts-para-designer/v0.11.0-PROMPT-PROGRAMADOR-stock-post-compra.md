# v0.11.0 — Stock post-compra: badges NO_LLEGO + edición de ítems + redirect desde compras

**VERSIÓN:** v0.11.0
**SLUG:** stock-post-compra
**TIPO:** Prompt de implementación (para agente programador / Cursor)
**DEPENDENCIAS:** v0.9.1 (fix imagen)
**ESTIMACIÓN:** 2-3h

---

## Contexto

Cuando el usuario crea una compra con `tipoCompra = EQUIPO` y `tipoEquipo` ∈ {DRON, BATERIA, HELICE},
el backend crea automáticamente el ítem en stock con `estado = NO_LLEGO`.

**El problema actual:**
1. Las vistas de stock no muestran ítems con `estado = NO_LLEGO` (no está en `ESTADO_LABELS`)
   → los ítems recién comprados quedan invisibles.
2. Las vistas de detalle no tienen formulario de edición → el admin no puede completar
   marca, modelo, número de serie, etc.
3. `ComprasView` no hace nada especial al crear una compra de equipo → el usuario no sabe
   que se generó un ítem en stock.

---

## PASO 0 — Contexto previo a leer

```
OBLIGATORIO:
- src/api/stock.js                            ← agregar updateItem() aquí
- src/api/index.js                            ← exportar updateItem
- src/views/stock/StockDronesView.vue         ← agregar NO_LLEGO + badges con color
- src/views/stock/StockBateriasView.vue       ← ídem
- src/views/stock/StockHelicesView.vue        ← ídem
- src/views/stock/StockDronDetalleView.vue    ← agregar formulario de edición
- src/views/stock/StockBateriaDetalleView.vue ← ídem
- src/views/stock/StockHeliceDetalleView.vue  ← ídem
- src/views/ComprasView.vue                   ← capturar resultado de crearCompra y redirigir

REFERENCIA:
- INFORME_BACKEND_PARA_FRONTEND.md §10 (enum Estado, TipoEquipo)
```

---

## PASO 1 — Agregar `updateItem()` en `src/api/stock.js`

Añadir al final del archivo:

```js
/**
 * Actualiza un ítem de equipo enviando el objeto completo con id.
 * El PUT acepta el objeto entero; los campos no enviados quedan en null.
 * @param {string} tipo - drones | baterias | helices (y otros en el futuro)
 * @param {Object} body - objeto completo del ítem incluyendo id
 * @returns {Promise<Object>}
 */
export async function updateItem(tipo, body) {
  const { data } = await api.put(`/${tipo}`, body)
  return data
}
```

---

## PASO 2 — Exportar `updateItem` en `src/api/index.js`

En la sección de exports de `stock.js`, agregar `updateItem`:

```js
export {
  getList,
  getById,
  updateItem,   // ← agregar
  getLicencias,
  getLicencia,
  getAccesorios,
  getAccesorio,
} from './stock.js'
```

---

## PASO 3 — Actualizar vistas de listado: StockDronesView, StockBateriasView, StockHelicesView

**Los tres archivos tienen la misma estructura.** Aplicar estos cambios a cada uno:

### 3.1 — Actualizar `ESTADO_LABELS` para incluir `NO_LLEGO` y `EN_MANTENIMIENTO`

```js
const ESTADO_LABELS = {
  NO_LLEGO:         'Pendiente de llegada',
  STOCK_ACTUAL:     'En stock',
  EN_PROCESO:       'En proceso',
  STOCK_ACTIVO:     'En operación',
  EN_MANTENIMIENTO: 'En mantenimiento',
  EN_DESUSO:        'En desuso',
}
```

### 3.2 — Agregar `ESTADO_CLASES` para mapear colores

```js
const ESTADO_CLASES = {
  NO_LLEGO:         'badge--yellow',
  STOCK_ACTUAL:     'badge--blue',
  EN_PROCESO:       'badge--blue',
  STOCK_ACTIVO:     'badge--green',
  EN_MANTENIMIENTO: 'badge--orange',
  EN_DESUSO:        'badge--gray',
}

function claseEstado(estado) {
  return ESTADO_CLASES[estado] || 'badge--gray'
}
```

### 3.3 — Actualizar el select de filtros para incluir los nuevos estados

El `<select v-model="filtroEstado">` ya itera `ESTADO_LABELS` dinámicamente —
al actualizar el objeto, los nuevos estados aparecen automáticamente sin cambios en el template.

### 3.4 — Reemplazar el texto plano de estado por un badge con color

Buscar en el template la línea:
```html
<div v-if="item.estado" class="item-card__estado">{{ labelEstado(item.estado) }}</div>
```

Reemplazar por:
```html
<div v-if="item.estado" :class="['item-card__badge', claseEstado(item.estado)]">
  {{ labelEstado(item.estado) }}
</div>
```

### 3.5 — Agregar estilos de badge en `<style scoped>`

Reemplazar o complementar `.item-card__estado` con las nuevas clases:

```css
.item-card__badge {
  display: inline-block;
  margin-top: 0.5rem;
  padding: 0.2rem 0.6rem;
  border-radius: 999px;
  font-size: 0.75rem;
  font-weight: 600;
}
.badge--yellow  { background: #fef9c3; color: #854d0e; }
.badge--blue    { background: #dbeafe; color: #1e40af; }
.badge--green   { background: #dcfce7; color: #166534; }
.badge--orange  { background: #ffedd5; color: #9a3412; }
.badge--gray    { background: #f1f5f9; color: #475569; }
```

---

## PASO 4 — Actualizar vistas de detalle: StockDronDetalleView, StockBateriaDetalleView, StockHeliceDetalleView

**Los tres archivos tienen la misma estructura.** Aplicar estos cambios a cada uno.
Tener en cuenta que el `TIPO` varía: `'drones'` / `'baterias'` / `'helices'`.

### 4.1 — Agregar imports necesarios

```js
import { getById, updateItem } from '../../api'
```

### 4.2 — Actualizar `ESTADO_LABELS` igual que en el listado

```js
const ESTADO_LABELS = {
  NO_LLEGO:         'Pendiente de llegada',
  STOCK_ACTUAL:     'En stock',
  EN_PROCESO:       'En proceso',
  STOCK_ACTIVO:     'En operación',
  EN_MANTENIMIENTO: 'En mantenimiento',
  EN_DESUSO:        'En desuso',
}
```

### 4.3 — Agregar estado del formulario de edición

```js
const editModal = ref({
  open: false,
  loading: false,
  apiError: '',
  // campos editables
  nombre: '',
  marca: '',
  modelo: '',
  numeroSerie: '',
  garantia: '',
  estado: '',
})

const toast = ref('')
let toastTimer = null
function showToast(msg) {
  toast.value = msg
  clearTimeout(toastTimer)
  toastTimer = setTimeout(() => { toast.value = '' }, 4000)
}
```

### 4.4 — Funciones de edición

```js
function openEdit() {
  const o = item.value
  editModal.value = {
    open: true,
    loading: false,
    apiError: '',
    nombre: o.nombre || '',
    marca: o.marca || '',
    modelo: o.modelo || '',
    numeroSerie: o.numeroSerie || '',
    garantia: o.garantia || '',
    estado: o.estado || '',
  }
}

function closeEdit() {
  editModal.value.open = false
}

async function saveEdit() {
  editModal.value.loading = true
  editModal.value.apiError = ''
  try {
    const body = {
      ...item.value,                           // incluir todos los campos para no borrar nada
      nombre: editModal.value.nombre || item.value.nombre,
      marca: editModal.value.marca || null,
      modelo: editModal.value.modelo || null,
      numeroSerie: editModal.value.numeroSerie || null,
      garantia: editModal.value.garantia || null,
      estado: editModal.value.estado,
    }
    item.value = await updateItem(TIPO, body)
    closeEdit()
    showToast('Ítem actualizado correctamente.')
  } catch (e) {
    editModal.value.apiError = e.message || 'Error al guardar. Intentá de nuevo.'
  } finally {
    editModal.value.loading = false
  }
}
```

### 4.5 — Agregar botón "Editar" y badge de estado en el template

Dentro del bloque `<template v-else-if="item">`, en la sección `<div class="actions">`,
agregar un botón de edición:

```html
<div class="actions">
  <router-link :to="LISTA_ROUTE" class="btn-secondary">Volver al listado</router-link>
  <button class="btn-primary" @click="openEdit">
    {{ item.estado === 'NO_LLEGO' ? 'Completar datos' : 'Editar' }}
  </button>
</div>
```

### 4.6 — Agregar el modal de edición en el template

Al final del template (antes del cierre del `<div class="stock-detail-page">`):

```html
<!-- Modal de edición -->
<div v-if="editModal.open" class="modal-overlay" @click.self="closeEdit">
  <div class="modal-card">
    <h2 class="modal-title">{{ item.estado === 'NO_LLEGO' ? 'Completar datos del ítem' : 'Editar ítem' }}</h2>

    <div v-if="item.estado === 'NO_LLEGO'" class="modal-banner modal-banner--info">
      Este ítem fue generado automáticamente al registrar la compra.
      Completá los datos cuando el equipo llegue físicamente.
    </div>

    <div class="form-group">
      <label>Nombre</label>
      <input v-model="editModal.nombre" type="text" class="form-input" placeholder="Nombre del equipo" />
    </div>
    <div class="form-group">
      <label>Marca</label>
      <input v-model="editModal.marca" type="text" class="form-input" placeholder="Ej: DJI" />
    </div>
    <div class="form-group">
      <label>Modelo</label>
      <input v-model="editModal.modelo" type="text" class="form-input" placeholder="Ej: Matrice 4TD" />
    </div>
    <div class="form-group">
      <label>Nº de serie</label>
      <input v-model="editModal.numeroSerie" type="text" class="form-input" placeholder="Nº de serie" />
    </div>
    <div class="form-group">
      <label>Garantía</label>
      <input v-model="editModal.garantia" type="text" class="form-input" placeholder="Ej: 12 meses" />
    </div>
    <div class="form-group">
      <label>Estado</label>
      <select v-model="editModal.estado" class="form-input">
        <option v-for="(label, val) in ESTADO_LABELS" :key="val" :value="val">{{ label }}</option>
      </select>
    </div>

    <div v-if="editModal.apiError" class="modal-banner modal-banner--error">{{ editModal.apiError }}</div>

    <div class="modal-actions">
      <button class="btn-secondary" :disabled="editModal.loading" @click="closeEdit">Cancelar</button>
      <button class="btn-primary" :disabled="editModal.loading" @click="saveEdit">
        {{ editModal.loading ? 'Guardando…' : 'Guardar' }}
      </button>
    </div>
  </div>
</div>

<!-- Toast -->
<div v-if="toast" class="toast">{{ toast }}</div>
```

### 4.7 — Agregar estilos del modal y toast en `<style scoped>`

```css
.btn-primary { padding: 0.6rem 1.25rem; background: #0d7377; color: #fff; border: none; border-radius: 8px; cursor: pointer; font-size: 0.9rem; }
.btn-primary:hover:not(:disabled) { background: #0a5c60; }
.btn-primary:disabled { opacity: 0.6; cursor: not-allowed; }
.modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.4); display: flex; align-items: center; justify-content: center; z-index: 1000; }
.modal-card { background: #fff; border-radius: 12px; padding: 1.5rem; width: 100%; max-width: 480px; max-height: 90vh; overflow-y: auto; }
.modal-title { margin: 0 0 1rem; font-size: 1.1rem; font-weight: 700; color: #1e293b; }
.modal-banner { padding: 0.75rem 1rem; border-radius: 8px; font-size: 0.875rem; margin-bottom: 1rem; }
.modal-banner--info { background: #eff6ff; color: #1e40af; }
.modal-banner--error { background: #fef2f2; color: #dc2626; }
.form-group { margin-bottom: 1rem; }
.form-group label { display: block; font-size: 0.85rem; color: #475569; margin-bottom: 0.3rem; }
.form-input { width: 100%; padding: 0.5rem 0.75rem; border: 1px solid #e2e8f0; border-radius: 8px; font-size: 0.9rem; box-sizing: border-box; }
.modal-actions { display: flex; justify-content: flex-end; gap: 0.75rem; margin-top: 1.25rem; }
.toast { position: fixed; top: 1.5rem; right: 1.5rem; background: #1e293b; color: #fff; padding: 0.75rem 1.25rem; border-radius: 8px; font-size: 0.9rem; z-index: 2000; }
```

---

## PASO 5 — Modificar `ComprasView.vue`: redirect post-compra de equipo

En la función `saveCompra()`, la línea actual es:

```js
await crearCompra(body)
showToast('Compra creada exitosamente.')
```

Reemplazar por:

```js
const compraCreada = await crearCompra(body)
if (body.tipoCompra === 'EQUIPO') {
  const rutasStock = {
    DRON:    '/stock/drones',
    BATERIA: '/stock/baterias',
    HELICE:  '/stock/helices',
  }
  const destino = rutasStock[body.tipoEquipo]
  if (destino) {
    showToast(`Equipo agregado al stock con estado "Pendiente de llegada". Podés completar los datos desde el stock.`)
    closeFormModal()
    fetchCompras()
    setTimeout(() => router.push(destino), 1500)
    return
  } else {
    // DOCK, ANTENA_RTK, ANTENA_STARLINK: no se crea automáticamente
    showToast(`Compra registrada. El equipo de tipo ${body.tipoEquipo} debe crearse manualmente en el inventario.`)
  }
} else {
  showToast('Compra creada exitosamente.')
}
```

**Importante:** Verificar que `useRouter` esté importado y que `router` esté declarado en el `<script setup>` de `ComprasView.vue`. Si no existe, agregar:

```js
import { useRouter } from 'vue-router'
const router = useRouter()
```

---

## PASO 6 — Verificación

### Stock — listado
- [ ] `/stock/drones` muestra ítems con `estado = NO_LLEGO` con badge amarillo "Pendiente de llegada".
- [ ] Los demás estados muestran el badge con el color correcto (azul, verde, naranja, gris).
- [ ] El filtro por estado incluye "Pendiente de llegada" en las opciones.
- [ ] Misma verificación para `/stock/baterias` y `/stock/helices`.

### Stock — detalle
- [ ] Detalle de un ítem `NO_LLEGO` muestra botón "Completar datos".
- [ ] Detalle de cualquier ítem muestra botón "Editar".
- [ ] Modal de edición se abre con los datos actuales pre-cargados.
- [ ] Al guardar → PUT al backend → ítem actualizado → toast "Ítem actualizado correctamente."
- [ ] Error de API → mensaje en el modal, no se cierra.
- [ ] Misma verificación para batería y hélice.

### Compras — post-compra
- [ ] Al crear una compra con `tipoCompra = EQUIPO` y `tipoEquipo = DRON` → toast + redirect a `/stock/drones`.
- [ ] Ídem para BATERIA → `/stock/baterias`, HELICE → `/stock/helices`.
- [ ] Para DOCK/ANTENA → toast informativo, sin redirect.
- [ ] Compras no-EQUIPO → comportamiento anterior sin cambios.

### Calidad
- [ ] Build sin errores (`npm run build`).
- [ ] Sin errores en consola.
- [ ] Textos en español.

---

## PASO N — Commit y tag

```bash
VERSION="v0.11.0"
SLUG="stock-post-compra"

git add src/api/stock.js \
        src/api/index.js \
        src/views/stock/StockDronesView.vue \
        src/views/stock/StockBateriasView.vue \
        src/views/stock/StockHelicesView.vue \
        src/views/stock/StockDronDetalleView.vue \
        src/views/stock/StockBateriaDetalleView.vue \
        src/views/stock/StockHeliceDetalleView.vue \
        src/views/ComprasView.vue

git commit -m "feat(stock): badges NO_LLEGO, edición de ítems y redirect post-compra de equipo

- Stock listado (drones/baterias/helices): badge coloreado por estado, incluye NO_LLEGO
- Stock detalle: modal de edición con PUT al backend; botón 'Completar datos' para NO_LLEGO
- ComprasView: redirect a stock correspondiente al crear compra de tipo EQUIPO
- DOCK/ANTENA: mensaje informativo sin redirect (requieren alta manual)
- API: updateItem() en stock.js"

git tag -a "${VERSION}" -m "Release ${VERSION}: Stock post-compra"
```

---

## Notas para el programador

1. **PUT acepta el objeto completo** — el backend espera el Dron/Batería/Hélice completo con `id`.
   En `saveEdit()`, usar spread `{ ...item.value, ...camposEditados }` para no borrar campos
   que no están en el formulario (ej. `compra`, `site`, etc.).

2. **Los tres listados y los tres detalles son casi idénticos** — cambiar solo `TIPO`,
   `TITULO`, `LISTA_ROUTE` e `IMAGEN`. El resto del código es igual entre los tres.

3. **El toast + redirect en ComprasView** — el `setTimeout` de 1500ms da tiempo al usuario
   de leer el toast antes de ser redirigido. Si se prefiere un botón "Ver en stock" en lugar
   de redirect automático, es igualmente válido.

4. **`NO_LLEGO` en filtros** — como `ESTADO_LABELS` se itera dinámicamente en el select,
   solo hay que agregar la clave al objeto y aparece automáticamente.
